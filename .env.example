WORLD_CUP_DATA_MODE=processed
# Copy to repo-root `.env` for API + web, or keep in apps/web/.env.local for local dev.
WCO_ADMIN_SYNC_KEY=replace-with-a-long-random-secret
FOOTBALL_DATA_API_TOKEN=replace-with-your-football-data-token
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
